# =============================================================================
# p003 spine2 - spine SR Linux | AS 65600 | system0 10.1.0.2
# Base: configs/srl/spine2.cfg (k01), IPs/AS do envelope S003.
# =============================================================================
enter candidate

# ---------------------------------------------------------------- jumbo MTU
set / system mtu default-port-mtu 9232
set / system mtu default-l2-mtu 9232
set / system mtu default-ip-mtu 9194
set / system mtu min-path-mtu 9212

# ------------------------------------------------- interfaces para os leaves
set / interface ethernet-1/1 admin-state enable
set / interface ethernet-1/1 description "to leaf1"
set / interface ethernet-1/1 subinterface 0 admin-state enable
set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
set / interface ethernet-1/1 subinterface 0 ipv4 address 10.254.2.0/31
set / interface ethernet-1/2 admin-state enable
set / interface ethernet-1/2 description "to leaf2"
set / interface ethernet-1/2 subinterface 0 admin-state enable
set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
set / interface ethernet-1/2 subinterface 0 ipv4 address 10.254.4.0/31
set / interface ethernet-1/3 admin-state enable
set / interface ethernet-1/3 description "to leaf3"
set / interface ethernet-1/3 subinterface 0 admin-state enable
set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
set / interface ethernet-1/3 subinterface 0 ipv4 address 10.254.6.0/31

# ------------------------------------------------------------------ loopback
set / interface system0 admin-state enable
set / interface system0 subinterface 0 admin-state enable
set / interface system0 subinterface 0 ipv4 admin-state enable
set / interface system0 subinterface 0 ipv4 address 10.1.0.2/32

# --------------------------------------------------------- network-instance
set / network-instance default admin-state enable
set / network-instance default interface ethernet-1/1.0
set / network-instance default interface ethernet-1/2.0
set / network-instance default interface ethernet-1/3.0
set / network-instance default interface system0.0

# ------------------------------------------------------------------ policies
set / routing-policy policy all default-action policy-result accept

# ----------------------------------------------------------------------- BGP
set / network-instance default protocols bgp admin-state enable
set / network-instance default protocols bgp autonomous-system 65600
set / network-instance default protocols bgp router-id 10.1.0.2

set / network-instance default protocols bgp afi-safi ipv4-unicast admin-state enable
set / network-instance default protocols bgp afi-safi ipv4-unicast multipath allow-multiple-as true
set / network-instance default protocols bgp afi-safi ipv4-unicast multipath ibgp maximum-paths 64
set / network-instance default protocols bgp afi-safi ipv4-unicast multipath ebgp maximum-paths 64

set / network-instance default protocols bgp group leafs admin-state enable
set / network-instance default protocols bgp group leafs description "underlay eBGP para leaves"
set / network-instance default protocols bgp group leafs export-policy [ all ]
set / network-instance default protocols bgp group leafs import-policy [ all ]
set / network-instance default protocols bgp group leafs afi-safi ipv4-unicast admin-state enable
set / network-instance default protocols bgp group leafs timers hold-time 9
set / network-instance default protocols bgp group leafs timers keepalive-interval 3

set / network-instance default protocols bgp neighbor 10.254.2.1 admin-state enable
set / network-instance default protocols bgp neighbor 10.254.2.1 description "leaf1"
set / network-instance default protocols bgp neighbor 10.254.2.1 peer-group leafs
set / network-instance default protocols bgp neighbor 10.254.2.1 peer-as 65201
set / network-instance default protocols bgp neighbor 10.254.4.1 admin-state enable
set / network-instance default protocols bgp neighbor 10.254.4.1 description "leaf2"
set / network-instance default protocols bgp neighbor 10.254.4.1 peer-group leafs
set / network-instance default protocols bgp neighbor 10.254.4.1 peer-as 65202
set / network-instance default protocols bgp neighbor 10.254.6.1 admin-state enable
set / network-instance default protocols bgp neighbor 10.254.6.1 description "leaf3"
set / network-instance default protocols bgp neighbor 10.254.6.1 peer-group leafs
set / network-instance default protocols bgp neighbor 10.254.6.1 peer-as 65203

commit now
